# 🏆 Blinkoff IoT Platform

**Тип:** Scalable Secure IoT Platform with Event-Driven Architecture.
**Статус:** Distributed State (Redis) & Reactive Notifications (Kafka).

Высокопроизводительная платформа для управления IoT-устройствами, построенная на принципах **Stateless Architecture**. Система обеспечивает безопасный обмен данными в реальном времени, отказоустойчивость и мгновенную реакцию на аварийные события.

## 🏛 Архитектура

### 1. Stateless Core & Distributed State

Ядро системы не хранит состояние, что позволяет горизонтально масштабировать серверную часть.

* **Hot Data (Redis):** Оперативные данные (текущая телеметрия, статус `Online`, `LastSeen`, WhiteList устройств) хранятся в **Redis**. Это обеспечивает сохранение состояния при перезагрузке сервера и доступность данных для всех инстансов приложения.
* **Token Management:** Временные токены привязки (OTP) используют механизм **Redis TTL** для автоматического удаления.
* **Cold Data (PostgreSQL):** Исторические данные и конфигурация синхронизируются в реляционную БД через фоновые процессы (Write-Behind).

### 2. Event-Driven & Monitoring

* **Apache Kafka:** Выступает магистралью событий (`iot-alarms`). Критические изменения (ошибки устройств, подключение, разрыв связи) публикуются в топик для обработки внешними потребителями (Telegram Bot, Analytics).
* **Smart Watchdog:** Сторожевой процесс анализирует **Redis** на наличие "молчащих" устройств (Timeouts) и генерирует события `CONNECTION_LOST` или `CONNECTION_NOT_FOUND`, обеспечивая точный мониторинг доступности.

### 3. Security First

* **Traffic Encryption:** Весь payload устройств шифруется по стандарту **AES-128 (GCM)**.
* **Device Auth:** Валидация `X-Chip-Id` на уровне TCP-рукопожатия через быстрый кэш в Redis (Set).
* **Admin API:** Защита административных эндпоинтов через API Key.

---

## 🛠 Технологический стек

### Backend

* **Language:** Java 21
* **Framework:** Spring Boot 3.4 (Web, WebSocket, Data Redis, Kafka)
* **Build Tool:** Maven

### Infrastructure & Storage

* **In-Memory Store:** Redis 7 (Hash, Set, String + TTL) — *State & Cache*
* **Message Broker:** Apache Kafka — *Event Streaming*
* **Database:** PostgreSQL 15 (`JSONB`) — *Persistence*
* **Containerization:** Docker & Docker Compose

### Protocols

* **WebSocket (Secure):** Двусторонний канал для устройств (Encrypted JSON).
* **HTTP REST:** API для управления и интеграций.

### Testing

* **JUnit 5 & Mockito:** Unit-тестирование бизнес-логики.
* **Testcontainers:** Честные интеграционные тесты с поднятием Redis, Kafka и PostgreSQL в Docker-контейнерах.

---

## 🔄 1. Сценарии Взаимодействия (Detailed Flows)

### 🔵 Сценарий А: Жизненный цикл устройства (Device Lifecycle)

1. **Handshake (Подключение):**
* Устройство открывает сокет `ws://host/ws/device`.
* Header: `X-Chip-Id: ESP-01`.
* **Core:** Проверяет наличие ID в Redis Set `device:auth:whitelist` (команда `SISMEMBER`).
* *Результат:* Если ОК — соединение открыто. Если нет — разрыв (Close Status 4003).
* **Event:** Core отправляет `CONNECTION_ESTABLISHED` в Kafka.


2. **Telemetry (Обработка данных):**
* Устройство шлет **AES-GCM** шифрованный Payload.
* **Core:** Дешифрует JSON.
* **Core (Redis Update):** Обновляет Hash `device:state:{chipId}` (команда `HMSET`):
    `params`: сырой JSON.
    `isOnline`: `true`.
    `lastSeen`: текущий Timestamp (long).

  **Analysis:** Парсит JSON на наличие ключей `errors` и `events`.
    Если массив не пуст -> **Kafka Producer** отправляет `DEVICE_ERROR` или `DEVICE_EVENT`.

3. **Disconnection (Разрыв):**
* Если сокет закрыт корректно или по ошибке сети (TCP FIN/RST).
* **Core:** Ставит `isOnline: false` в Redis.
* **Event:** Отправляет `CONNECTION_BROKEN` в Kafka.



### 🔴 Сценарий Б: Мониторинг "Мертвых душ" (Watchdog)

*Работает фоновый процесс (Scheduler), каждые 10 секунд.*

1. **Fetch:** Загружает список всех разрешенных устройств из Redis Set `device:auth:whitelist` (команда `SMEMBERS`).
2. **Check Loop:** Для каждого `chipId`:
* Читает состояние из Redis Hash `device:state:{chipId}` (`HMGET`: `isOnline`, `lastSeen`).
* Вычисляет `silence = now - lastSeen`.

3. **Logic:**
* **Случай 1 (Timeout):** Если `isOnline == true` И `silence > 60 sec`:
    **Action:** Пишет в Redis `isOnline = false` (`HSET`).
    **Event:** Шлет в Kafka `CONNECTION_LOST_TIMEOUT`.

* **Случай 2 (Still Dead):** Если `isOnline == false` И `silence > 60 sec`:
    **Action:** Ничего не пишет в Redis (статус уже false).
    **Event:** Шлет в Kafka `CONNECTION_NOT_FOUND` (Напоминание/Heartbeat ошибки).

### 🟣 Сценарий В: Маршрутизация Уведомлений (Bot)

1. **Listen:** Бот слушает топик `iot-alarms`.
2. **Enrich:** Получив ивент с `chipId`, Бот делает HTTP-запрос к Core: `GET /api/devices/{chipId}/owners`.
    *Важно:* Бот использует системный `X-Admin-Key` для этого запроса.
3. **Send:** Core возвращает список `chatId` (например `["12345", "67890"]`). Бот отправляет сообщение каждому.

---

## 📡 2. API Контракт (HTTP REST)

Все запросы возвращают `Content-Type: application/json`.

### 🔐 Auth Headers

* **User Context:** Обычно передается `userId` в query/body (для упрощения MVP) или Bearer Token (в будущем).
* **System Context:** `X-Admin-Key: <SECRET>` — полный доступ (для Бота и Админа).

### 🛠 Group 1: Provisioning & Admin (Требует X-Admin-Key)

Эти методы использует **Админ** (через Postman/Curl) или **Бот** (для генерации токенов).

| Method | Endpoint | Query / Body | Описание |
| --- | --- | --- | --- |
| `POST` | `/api/devices/provision` | **Body:**<br>

<br>`{ "chipId": "ESP-01", "name": "Incubator 1" }` | **Регистрация.** Добавляет ID в БД и в Redis Whitelist (`SADD`). |
| `POST` | `/api/devices/{id}/block` | - | **Блокировка.** `isActive=false`, удаление из Redis Whitelist (`SREM`), удаление токенов. |
| `POST` | `/api/devices/{id}/unblock` | - | **Разблокировка.** Возврат в Whitelist. |
| `POST` | `/api/devices/{id}/token` | - | **Генерация токена.** Создает UUID в Redis с TTL 5 мин. Возвращает: `{ "token": "uuid..." }` |
| `GET` | `/api/devices/{id}/owners` | **Query:** `platform=TELEGRAM`<br>

<br>*(Optional)* | **Поиск владельцев.** Возвращает список ID: `["112233", "445566"]`. Используется Ботом при получении алертов. |

### 👤 Group 2: User Binding & Data (Публичные/Bot)

Эти методы использует **Бот** (от имени юзера) или **Мобильное приложение**.

| Method | Endpoint | Query / Body | Описание |
| --- | --- | --- | --- |
| `POST` | `/api/bindings` | **Body:**<br>

<br>`{ "token": "uuid...", "userId": "tg_123", "platform": "TELEGRAM" }` | **Привязка.** Проверяет токен в Redis (`GETDEL`). Создает запись в БД. |
| `DELETE` | `/api/bindings` | **Query:** `chipId=ESP-01`<br>

<br>**Query:** `userId=tg_123` | **Отвязка.** Удаляет связь пользователя с устройством. |
| `GET` | `/api/devices` | **Query:** `userId=tg_123` | **Мои устройства.** Возвращает список `DeviceSummaryDto`. Имя устройства берется из Redis (если задано в телеметрии), иначе из БД. |
| `GET` | `/api/devices/{id}/telemetry` | **Query:** `userId=tg_123` | **Телеметрия.** Возвращает полный JSON состояния (`params` из Redis). |

---

## ⚡ 3. WebSocket Contract (Device Protocol)

* **URL:** `ws://host:8080/ws/device`
* **Security:**
* Header при подключении: `X-Chip-Id: <CHIP_ID>`
* Payload шифрование: **AES-128-GCM** (Shared Key прошит в прошивке).



### JSON Structure (Decrypted)

Сервер ожидает JSON. Поля `errors` и `events` триггерят Kafka. Пустые массивы `[]` означают "Ошибок нет / События закончились".

```json
{
  // 1. Произвольные данные (сохраняются в Redis "params")
  "temp": 37.5,
  "hum": 60.0,
  "fan_speed": 1200,
  
  // 2. Опционально: переименование в RAM
  "name": "Инкубатор #5", 

  // 3. Алертинг (Триггер Kafka: DEVICE_ERROR)
  "errors": [
    "Sensor Failure",
    "Heater Overload"
  ],

  // 4. События (Триггер Kafka: DEVICE_EVENT)
  "events": [
    "Door Opened",
    "Cycle Finished"
  ]
}

```

---

## 📣 4. Kafka Event Contract

* **Topic:** `iot-alarms`
* **Serialization:** JSON

Используется для асинхронной связи между Core и Bot.

### Payload Structure (`DeviceAlarmEvent`)

```json
{
  "chipId": "ESP-TEST-01",
  "type": "CONNECTION_LOST_TIMEOUT",
  "message": "Device Timeout (Silent for 65s)",
  "payload": null,  // Или массив строк ["Error 1", "Error 2"] для типа DEVICE_ERROR
  "timestamp": "2025-12-19T10:00:00Z"
}

```

### Event Types Reference

| Тип | Описание | Триггер |
| --- | --- | --- |
| `CONNECTION_ESTABLISHED` | Устройство подключилось | WebSocket Handshake |
| `CONNECTION_BROKEN` | Разрыв соединения (TCP) | WebSocket Close |
| `CONNECTION_LOST_TIMEOUT` | Таймаут (был Online -> стал Offline) | Watchdog (Redis lastSeen > 60s) |
| `CONNECTION_NOT_FOUND` | Устройство давно офлайн (напоминание) | Watchdog (Redis lastSeen > 60s && Offline) |
| `DEVICE_ERROR` | Ошибки датчиков/оборудования | JSON поле `"errors": [...]` |
| `DEVICE_EVENT` | Информационные события | JSON поле `"events": [...]` |

### 📂 Подробное описание структуры и классов

```text
src/main/java/com/blinkoff/iot
├── BlinkoffIoTApplication.java         // 🚦 Точка входа. Содержит @EnableScheduling, чтобы Spring запускал фоновые процессы (Watchdog и SyncService).
│
├── shared                              // 🧱 ОБЩЕЕ ЯДРО (Утилиты и Конфигурация)
│   ├── config
│   │   ├── AppConstants.java           // 📏 Константы: лимиты устройств на пользователя, таймауты токенов, имена топиков Kafka.
│   │   ├── RedisConfig.java            // 🔥 NEW: Конфигурация RedisTemplate. Настраивает JSON-сериализацию, чтобы в Redis Commander данные были читаемыми, а не бинарным мусором.
│   │   ├── WebConfig.java              // ⚙️ Настройка MVC: регистрирует перехватчики (AdminAuthInterceptor) для защиты API.
│   │   ├── WebSocketConfig.java        // 🔌 Регистрация WebSocket эндпоинта (/ws/device) и привязка к нему DeviceHandler.
│   │   └── DataSeeder.java             // 🌱 Загрузчик данных: при старте проверяет БД и создает тестового админа/устройство, если база пуста.
│   ├── exception                       // ⚠️ Обработка ошибок
│   │   ├── ApiError.java               // DTO для красивого JSON-ответа при ошибке (timestamp, status, message).
│   │   ├── GlobalExceptionHandler.java // @ControllerAdvice: ловит исключения (например, InvalidTokenException) и превращает их в ApiError.
│   │   └── InvalidTokenException.java  // Кастомное исключение: кидается, если токен привязки не найден в Redis.
│   └── security
│       ├── AdminAuthInterceptor.java   // 🛡️ Перехватчик HTTP. Проверяет заголовок X-Admin-Key. Защищает админку и методы для Бота.
│       └── crypto                      // 🔐 Шифрование
│           ├── AesCryptoEngine.java    // Реализация AES-128 (GCM). Дешифрует входящие сообщения от устройств.
│           ├── KeyProvider.java        // Интерфейс для получения ключей шифрования.
│           └── StaticKeyProvider.java  // Реализация: берет ключи из application.yaml (позже можно заменить на Vault).
│
└── modules                             // 📦 БИЗНЕС-МОДУЛИ
    │
    ├── device_management               // 🛠 УПРАВЛЕНИЕ (Холодные данные и API)
    │   ├── controller
    │   │   ├── DeviceBindingController.java      // API (User): POST /bind (привязать по токену), DELETE /bind (отвязать).
    │   │   ├── DeviceDataController.java         // API (User): GET /devices. Возвращает список устройств юзера, объединяя данные из БД и Redis.
    │   │   └── DeviceProvisioningController.java // API (Admin/Bot): Создание чипа, блок/разблок, генерация токена, получение владельцев.
    │   ├── service
    │   │   ├── DeviceProvisioningService.java  // Логика "Завода": создает Device в PostgreSQL, обновляет "Белый список" в Redis.
    │   │   ├── DeviceBindingService.java       // Логика "Связей": проверяет токен через TokenStore, создает запись в таблице bindings.
    │   │   └── DeviceDataService.java          // Агрегатор: берет имя из БД, статус из Redis, температуру из Redis -> собирает DTO.
    │   ├── store
    │   │   └── ProvisioningTokenStore.java     // 🔥 UPD: Хранилище временных токенов в Redis. Использует TTL (Time-To-Live), чтобы Redis сам удалял протухшие токены.
    │   ├── dto
    │   │   ├── BindRequest.java        // JSON: { token, userId, platform }
    │   │   ├── DeviceSummaryDto.java   // JSON для списка устройств (кратко).
    │   │   ├── DeviceTelemetryDto.java // JSON для детального вида (полная телеметрия).
    │   │   ├── ProvisionRequest.java   // JSON для регистрации чипа.
    │   │   └── ProvisionResponse.java  // Ответ после регистрации.
    │   ├── model
    │   │   ├── enums/PlatformType.java // Enum: TELEGRAM, ANDROID (откуда пришел юзер).
    │   │   ├── enums/AccessRole.java   // Enum: OWNER, VIEWER (права доступа).
    │   │   ├── Device.java             // JPA Entity: таблица devices.
    │   │   └── DeviceBinding.java      // JPA Entity: таблица bindings (Many-to-Many связь Users <-> Devices).
    │   └── repository
    │       ├── DeviceBindingRepository.java // SQL: поиск владельцев, фильтрация по платформе, проверка прав.
    │       └── DeviceRepository.java        // SQL: CRUD для устройств.
    │
    ├── notification                    // 🔔 УВЕДОМЛЕНИЯ (Event Bus)
    │   ├── event
    │   │   └── DeviceAlarmEvent.java   // 📨 DTO события: ChipId, Тип (ERROR/EVENT/LOST), Payload (список ошибок).
    │   └── kafka
    │       └── AlarmProducer.java      // 📣 Продюсер: отправляет DeviceAlarmEvent в топик 'iot-alarms'.
    │
    └── telemetry                       // 🌡 ТЕЛЕМЕТРИЯ (Горячие данные и WebSocket)
        ├── api
        │   └── device_facing
        │       ├── DeviceHandler.java              // WebSocket логика: 1. Принять 2. Расшифровать 3. Обновить Redis 4. Если есть ошибки -> Kafka.
        │       └── DeviceHandshakeInterceptor.java // "Вышибала": проверяет X-Chip-Id перед соединением. Смотрит в Redis Whitelist.
        ├── service
        │   └── DeviceAuthCache.java    // 🔥 UPD: Обертка над Redis Set ('device:auth:whitelist'). Быстрая проверка "Разрешен ли этот чип?".
        ├── store
        │   └── RedisDeviceStateStore.java // 🔥 NEW: Реализация хранилища состояния на Redis Hash. Ключи: 'device:state:{id}'. Поля: params, isOnline, lastSeen.
        ├── engine
        │   ├── StateSyncService.java           // 🕰 Синхронизатор: Раз в N секунд берет данные из Redis и сохраняет их в PostgreSQL (для истории/аналитики).
        │   └── DeviceConnectivityWatchdog.java // 🐕 Сторож: Раз в 10 сек сканирует Redis. Если lastSeen старый -> ставит Offline в Redis -> шлет Alert в Kafka.
        ├── model
        │   └── DeviceState.java        // JPA Entity: таблица device_states (JSONB params, last_seen).
        └── repository
            └── DeviceStateRepository.java // SQL: сохранение состояний в БД.
```

-----

### 🔄 Подробные Сценарии Взаимодействия (Interaction Flows)

#### 1. 🚀 Старт Системы (System Warmup)

*Цель: Синхронизировать Redis ("горячую" память) с PostgreSQL ("холодным" хранилищем) при запуске сервера, чтобы Watchdog не считал все устройства мертвыми.*

1. **Boot:** Spring запускает `BlinkoffIoTApplication`.
2. **Auth Cache Warmup:**
* `DeviceAuthCache` (@PostConstruct) запрашивает у `DeviceRepository` список всех активных `chip_id`.
* Записывает их в **Redis Set** (`device:auth:whitelist`).
3. **State Warmup:**
* `StateSyncService` (@PostConstruct) запрашивает у `DeviceStateRepository` последние известные состояния.
* Вызывает `RedisDeviceStateStore`, который заполняет **Redis Hash** (`device:state:{id}`) данными (`params`, `lastSeen`), но ставит `isOnline = false` (так как сокеты еще не подняты).

---

#### 2. 🔌 Подключение Устройства (Device Connection Handshake)

*Цель: Авторизовать устройство и уведомить систему о подключении.*

1. **Request:** ESP32 открывает WebSocket соединение с заголовком `X-Chip-Id`.
2. **Intercept:** `DeviceHandshakeInterceptor` перехватывает запрос до открытия сессии.
3. **Check:** Интерцептор обращается к `DeviceAuthCache`.
* Тот делает запрос в **Redis**: `SISMEMBER device:auth:whitelist <chipId>`.
* *Результат:* Если `false` — соединение сбрасывается (403 Forbidden).


4. **Session Open:** Если проверка пройдена, управление передается в `DeviceHandler`.
5. **State Update:** `DeviceHandler` вызывает `RedisDeviceStateStore.setOnline(true)`.
6. **Event:** `DeviceHandler` использует `AlarmProducer`, чтобы отправить событие `CONNECTION_ESTABLISHED` в Kafka.

---

#### 3. 🌡 Обработка Телеметрии (Telemetry Processing)

*Цель: Принять данные, сохранить в "горячую" память и проверить на аварии.*

1. **Incoming Message:** `DeviceHandler` получает зашифрованный текст через WebSocket.
2. **Decrypt:** `DeviceHandler` передает текст в `AesCryptoEngine`. Тот возвращает чистый JSON.
3. **Save Hot State:**
* `DeviceHandler` вызывает `RedisDeviceStateStore.updateParams(json)`.
* Стор выполняет команду Redis `HMSET` (обновляет `params`, ставит `lastSeen = now()`, `isOnline = true`).
4. **Alarm Analysis:**
* `DeviceHandler` парсит JSON.
* Если массив `errors` не пуст -> вызывает `AlarmProducer` (тип `DEVICE_ERROR`).
* Если массив `events` не пуст -> вызывает `AlarmProducer` (тип `DEVICE_EVENT`).
* Все события улетают в топик `iot-alarms`.

---

#### 4. 🐕 Мониторинг Связи (Watchdog Cycle)

*Цель: Обнаружить "молчащие" устройства.*

1. **Trigger:** `@Scheduled` запускает метод `checkConnectivity()` в `DeviceConnectivityWatchdog` (каждые 10 сек).
2. **Fetch List:** Watchdog запрашивает список всех чипов у `DeviceAuthCache` (Redis: `SMEMBERS`).
3. **Check Status:** Для каждого чипа Watchdog запрашивает у `RedisDeviceStateStore` поля `lastSeen` и `isOnline`.
4. **Logic:**
* **Сценарий А (Таймаут):** Устройство `isOnline=true`, но `now - lastSeen > 60 sec`.
* Watchdog вызывает `RedisDeviceStateStore.setOnline(false)`.
* Watchdog вызывает `AlarmProducer` -> Kafka (`CONNECTION_LOST_TIMEOUT`).
* **Сценарий Б (Призрак):** Устройство `isOnline=false` и давно молчит.
* Watchdog вызывает `AlarmProducer` -> Kafka (`CONNECTION_NOT_FOUND`) для циклического напоминания (Heartbeat of failure).

---

#### 5. 💾 Синхронизация с БД (Persisting State)

*Цель: Периодически сохранять историю в PostgreSQL, не нагружая базу каждым пакетом телеметрии.*

1. **Trigger:** `@Scheduled` запускает `StateSyncService` (например, раз в 30 сек).
2. **Snapshot:** Сервис запрашивает у `RedisDeviceStateStore` все текущие состояния (Redis: `SCAN` или `KEYS`).
3. **Write:** Сервис формирует Batch-запрос и сохраняет сущности `DeviceState` в PostgreSQL через `DeviceStateRepository`.

---

#### 6. 🛠 Регистрация нового устройства (Admin Provisioning)

*Цель: Добавить новое устройство в систему.*

1. **API Call:** Админ шлет `POST /api/devices/provision` с заголовком `X-Admin-Key`.
2. **Security:** `AdminAuthInterceptor` сверяет ключ.
3. **Persist (Cold):** `DeviceProvisioningService` создает запись в `DeviceRepository` (Postgres).
4. **Persist (Hot):** Сервис сразу добавляет ChipId в `DeviceAuthCache` (Redis: `SADD device:auth:whitelist`).
* *Результат:* Устройство может подключаться сразу, перезагрузка сервера не требуется.

---

#### 7. 🔗 Привязка Пользователя (Token Binding)

*Цель: Безопасно связать Telegram-аккаунт с устройством.*

1. **Token Gen:** Админ вызывает `POST /api/devices/{id}/token`.
* `ProvisioningTokenStore` генерирует UUID.
* Сохраняет в Redis: `SET provisioning:token:{uuid} {chipId} EX 300` (TTL 5 минут).
2. **User Input:** Пользователь отправляет токен Боту. Бот шлет `POST /api/bindings`.
3. **Validation:**
* `DeviceBindingService` вызывает `ProvisioningTokenStore.consumeToken(token)`.
* Стор делает Redis `GETDEL` (Получить и удалить атомарно). Если вернулся `null` — ошибка.
4. **Binding:** Если токен валиден, сервис создает запись в `DeviceBindingRepository` (Postgres).

---

#### 8. 📱 Просмотр Данных Пользователем (Hybrid Read)

*Цель: Показать пользователю список устройств с актуальными данными.*

1. **API Call:** Юзер (через приложение/бота) делает `GET /api/devices`.
2. **Aggregation:** `DeviceDataService` делает два параллельных запроса:
* **Postgres:** Получает список привязанных устройств (статичные данные: имя, роль).
* **Redis:** Получает текущие состояния (`params`, `isOnline`) через `RedisDeviceStateStore`.
3. **Merge:** Сервис объединяет данные.
* *Приоритет имени:* Если в Redis (в JSON params) есть поле `name`, оно перекрывает имя из Postgres.
4. **Response:** Возвращает `List<DeviceSummaryDto>`.

---

#### 9. 🔔 Доставка Уведомлений (Notification Loop)

*Цель: Оповестить пользователя об инциденте.*

1. **Kafka Consumer:** Внешний сервис (Бот) получает сообщение из топика `iot-alarms`.
2. **Enrichment:** Бот вызывает Core API: `GET /api/devices/{chipId}/owners?platform=TELEGRAM`.
3. **Lookup:**
* `DeviceProvisioningController` вызывает `DeviceBindingService`.
* Сервис идет в `DeviceBindingRepository` и находит все `user_id` (Chat ID) для этого чипа и платформы `TELEGRAM`.
4. **Delivery:** Бот отправляет сообщение пользователю в Telegram.

## 🧪 Архитектура Тестов (Актуальная)

```text
src/test/java/com/blinkoff/iot
├── AbstractRedisTest.java              // 🛠 BASE: Конфигурация Testcontainers (поднимает Redis для тестов).
│
├── shared
│   └── security
│       ├── AdminAuthInterceptorTest.java       // ✅ UNIT: Проверка доступа по ключу.
│       └── crypto
│           └── AesCryptoEngineTest.java        // ✅ UNIT: AES-GCM шифрование.
│
└── modules
    ├── device_management
    │   ├── service
    │   │   ├── DeviceProvisioningServiceTest.java // ✅ UNIT: Бизнес-логика (Mock Repos).
    │   │   ├── DeviceBindingServiceTest.java      // ✅ UNIT: Логика привязки и фильтрации.
    │   │   └── DeviceDataServiceTest.java         // ✅ UNIT: Сборка DTO.
    │   ├── store
    │   │   └── ProvisioningTokenStoreTest.java    // 🐳 REDIS: Проверка записи, чтения и TTL токенов.
    │   ├── controller
    │   │   └── DeviceProvisioningControllerTest.java // ✅ WEB: MockMvc тесты API.
    │   ├── repository
    │   │   ├── DeviceRepositoryTest.java          // 🐢 INTEG: JPA/SQL запросы.
    │   │   └── DeviceBindingRepositoryTest.java   // 🐢 INTEG: Constraints, кастомные query.
    │
    ├── notification
    │   └── kafka
    │       └── AlarmProducerTest.java             // ✅ UNIT: Проверка вызова KafkaTemplate (Mock).
    │
    └── telemetry
        ├── api
        │   └── device_facing
        │       └── DeviceHandlerTest.java         // ✅ UNIT: WebSocket логика + Mock RedisStore.
        ├── service
        │   └── DeviceAuthCacheTest.java           // 🐳 REDIS: Whitelist (Set operations).
        ├── store
        │   └── RedisDeviceStateStoreTest.java     // 🐳 REDIS: Проверка Hash-структур, JSON-сериализации.
        ├── engine
        │   ├── StateSyncServiceTest.java          // ✅ UNIT: Логика синхронизации (Mock Store & Repo).
        │   └── DeviceConnectivityWatchdogTest.java// ✅ UNIT: Логика таймаутов (Mock Store).
        └── repository
            └── DeviceStateRepositoryTest.java     // 🐢 INTEG: JPA запросы (Postgres).

```

### 🗝 Легенда:

* ✅ **UNIT:** Быстрые тесты с моками (`Mockito`).
* 🐢 **INTEG:** Тесты базы данных (`@DataJpaTest` + Testcontainers Postgres).
* 🐳 **REDIS:** Тесты хранилищ (`@SpringBootTest` + Testcontainers Redis).
* 🛠 **BASE:** Служебный класс для настройки окружения.
